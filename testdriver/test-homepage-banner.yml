version: 4.2.12
session: 67afcd2f4fc3867415940b7a
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Assert that the homepage banner is visible.
    commands:
      - command: assert
        expect: the homepage banner with "Chat & Sell With AI" is visible
  - prompt: >-
      Click on the banner to verify it is interactive or leads to another
      webpage.
    commands:
      - command: hover-text
        text: Chat & Sell With AI
        description: banner text to verify interactivity
        action: click
  - prompt: >-
      Assert that the expected page or message appears after interaction.  # Use
      the content of the file from the environment
    commands:
      - command: assert
        expect: the "Sign Up" page is visible
